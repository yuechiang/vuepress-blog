<template><h1 id="github-actions" tabindex="-1"><a class="header-anchor" href="#github-actions" aria-hidden="true">#</a> GitHub Actions</h1>
<ul>
<li>
<p>作用<br>
实现仓库自动化管理。</p>
</li>
<li>
<p>名词解释</p>
<ol>
<li>
<p>工作流程<br>
工作流程是一个可配置的自动化过程，它将运行一个或多个作业。 工作流程由签入到存储库的 YAML 文件定义，并在存储库中的事件触发时运行，也可以手动触发，或按定义的时间表触发。</p>
</li>
<li>
<p>事件<br>
事件是存储库中触发工作流程运行的特定活动。</p>
</li>
<li>
<p>Jobs<br>
作业是工作流程中的一组步骤，它们在同一运行器上执行。 每个步骤要么是将要执行的 shell 脚本，要么是将运行的操作 。</p>
</li>
<li>
<p>Action<br>
操作是 GitHub Actions 平台的自定义应用程序，用于执行复杂但经常重复的任务。</p>
</li>
<li>
<p>运行器<br>
运行器是在触发工作流程时运行工作流程的服务器。</p>
</li>
</ol>
</li>
</ul>
</template>
